ENVIRONMENT=development
DATABASE_URL=postgres://postgres:postgres@localhost:5432/catalog_db
RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# In production, paste the base64-encoded PEM or literal string for PUBLIC KEY ONLY.
# Catalog Service only verifies JWTs, so it doesn't need the private key.
JWT_PUBLIC_KEY=
